var gulp = require('gulp');


//Run the tests:
gulp.task('test', []);

//Set up a build task for the client:
gulp.task('build', []);

//The build task is the default task:
gulp.task('default', 'build');